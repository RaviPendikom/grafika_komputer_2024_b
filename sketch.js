// User defined variable
// EVENT
function setup() {
  createCanvas(600, 600);
  background(78, 19, 187);
}

function draw() {
  // fill(222, 90, 190, 200);
  rectMode(CENTER);
  square(mouseX, mouseY, 100);
}
